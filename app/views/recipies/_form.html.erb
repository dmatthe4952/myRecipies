<%= form_for @recipie do |f| %>
  <% if @recipie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipie.errors.count, "error") %> prohibited
          this recipie from being saved:
      </h2>
      <ul>
        <% @recipie.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>