<% if obj.errors.any? %>
    <div id="error_explanation" class="alert-danger col-md-8 col-md-offset-2">
      <h5><%= pluralize(obj.errors.count, "error") %> prohibited
          this recipe from being saved:
      </h5>
      <ul>
        <% obj.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
    
<%end%>